{
  "name": "the-biblical-world",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:data": "node scripts/validate-data.mjs",
    "validate:repo": "node scripts/check-static.mjs",
    "build:search": "node scripts/build-search-index.mjs",
    "prebuild": "npm run validate:data && npm run audit:batch4 && npm run audit:batch5 && npm run audit:batch6 && npm run audit:batch7 && npm run audit:batch8 && npm run audit:batch9 && npm run audit:batch10 && npm run validate:repo && npm run build:search",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "audit:batch4": "node scripts/audit-batch4.mjs",
    "audit:batch5": "node scripts/audit-batch5.mjs",
    "audit:batch6": "node scripts/audit-batch6.mjs",
    "audit:batch7": "node scripts/audit-batch7.mjs",
    "audit:batch8": "node scripts/audit-batch8.mjs",
    "audit:batch9": "node scripts/audit-batch9.mjs",
    "audit:batch10": "node scripts/audit-batch10.mjs"
  },
  "dependencies": {
    "maplibre-gl": "^5.0.0",
    "minisearch": "^7.0.0",
    "pmtiles": "^4.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^4.0.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@types/geojson": "^7946.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
